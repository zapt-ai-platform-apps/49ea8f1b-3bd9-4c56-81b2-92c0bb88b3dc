import React from 'react';
import Home from './pages/Home';

/**
 * Application routes
 * In a real app, this would use a router
 */
export function AppRoutes() {
  // For now, we just render the Home page
  return <Home />;
}