import React from 'react';

/**
 * Application providers
 * In a real app, this would include context providers, theme providers, etc.
 */
export function AppProviders({ children }) {
  return (
    <>
      {children}
    </>
  );
}